#
# ~/.bash_profile
#
[[ -f ~/.bashrc ]] && . ~/.bashrc

#while true; do start-dwl.sh; sleep 1; done

# Created by `pipx` on 2025-12-11 21:59:43
export PATH="$PATH:/home/ultrafeeder4/.local/bin"
emacs --daemon
